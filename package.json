{
  "name": "money-manager-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first asset and net-worth tracker WeChat Mini Program.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "ci": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "tdesign-miniprogram": "^1.14.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "miniprogram-api-typings": "^5.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
